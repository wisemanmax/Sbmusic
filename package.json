{
  "name": "slime-by-site",
  "version": "1.0.0",
  "private": true,
  "description": "SLIME BY — static, no-build site (HTML + assets/app.js + cms.js).",
  "scripts": {
    "check:js": "node --check assets/app.js && node --check assets/quest.js && node --check assets/monsters.js && node --check cms.js && node --check assets/analytics.js && node --check sw.js && node --check tests/e2e.mjs && node --check tests/governor.test.mjs && node --check tests/quest.sim.mjs && node --check tests/poke.sim.mjs",
    "serve": "python3 -m http.server 8000",
    "test:install": "playwright install --with-deps chromium",
    "test:governor": "node tests/governor.test.mjs",
    "test:quest": "node tests/quest.sim.mjs",
    "test:poke": "node tests/poke.sim.mjs",
    "test:e2e": "node tests/e2e.mjs",
    "test": "npm run check:js && npm run test:governor && npm run test:quest && npm run test:poke && npm run test:e2e"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
